# TransitFlow 系統規格書
> **版本**：v1.0 ｜ **日期**：2026-05-__ ｜ **撰寫者**：周思佑、羅小語

---

## 1. 系統概述

### 1.1 專案背景

> 📝 *簡述此系統的背景與開發動機（2–3 句）*

本系統為 IM2002 資料庫管理課程期末作業，目標為建構一個虛構雙網絡交通運營商「TransitFlow」的後端資料庫系統，並透過 AI 聊天助理讓使用者能以自然語言查詢交通資訊、訂票、退票等服務。

### 1.2 系統目標

- [ ] 建立可運行的 PostgreSQL 關聯式資料庫（含完整 Schema）
- [ ] 建立 Neo4j 圖形資料庫（含地鐵 / 國鐵路網）
- [ ] 整合 pgvector 向量資料庫（RAG 政策文件查詢）
- [ ] 實作所有 Agent 工具對應的 Query 函式
- [ ] 能透過 Gradio 介面正常與 AI 互動

### 1.3 系統範疇（Scope）

**包含：**
- 地鐵（Metro）與國鐵（National Rail）資訊查詢
- 國鐵訂票 / 取消訂票
- 乘客帳號登入 / 註冊
- 政策文件語意搜尋（RAG）
- 路線規劃與換乘建議
- 延誤影響分析

**不包含：**
- 即時班次更新
- 線上金流串接
- 行動裝置 APP

---

## 2. 系統架構

### 2.1 整體架構圖

```
使用者（瀏覽器）
    │
    ▼
Gradio UI（skeleton/ui.py）
    │
    ▼
LLM Agent（skeleton/agent.py）◄──── Gemini / Ollama
    │
    ├──► PostgreSQL :5433
    │       ├── 關聯式資料表（班表、訂票、使用者...）
    │       └── pgvector（政策文件向量）
    │
    └──► Neo4j :7688
            └── 地鐵 + 國鐵路網圖
```

### 2.2 技術選型

| 層級 | 技術 | 版本 | 說明 |
|------|------|------|------|
| 語言 | Python | 3.10+ | 主要開發語言 |
| 關聯式 DB | PostgreSQL | 15 | 結構化資料儲存 |
| 向量 DB | pgvector | — | 政策文件 RAG |
| 圖形 DB | Neo4j | 5.x | 路網圖與路徑搜尋 |
| LLM | Gemini / Ollama | — | 自然語言理解 |
| UI 框架 | Gradio | — | 聊天介面 |
| 容器化 | Docker Compose | — | 服務編排 |

### 2.3 LLM Provider 選擇

- [ ] **Gemini**（需要 `GEMINI_API_KEY`，向量維度：`3072`）
- [ ] **Ollama**（本機，免費，向量維度：`768`）

選擇：**Ollama**

---

## 3. 資料庫規格

### 3.1 PostgreSQL 關聯式 Schema

> 📝 *說明各資料表設計決策*

#### 3.1.1 資料表清單

| 資料表名稱 | 對應 JSON | 主鍵 | 說明 |
|-----------|-----------|------|------|
| `metro_stations` | metro_stations.json | `station_id` | 地鐵站基本資料 |
| `metro_lines` 或 `station_lines` | metro_stations.json | `station_id`, `line` | 站別所屬路線（正規化或 ARRAY） |
| `metro_schedules` | metro_schedules.json | `schedule_id` | 地鐵班表 |
| `metro_schedule_stops` | metro_schedules.json | `schedule_id`,`station_id` | 班表停靠站（正規化） |
| `national_rail_stations` | national_rail_stations.json | `station_id` | 國鐵站基本資料 |
| `national_rail_schedules` | national_rail_schedules.json | `schedule_id` | 國鐵班表 |
| `national_rail_stops` | national_rail_schedules.json | `schedule_id`, `station_id` | 國鐵停靠站與時刻 |
| `national_rail_fares` | national_rail_schedules.json | `schedule_id`, `fare_class`| 國鐵票價區間 |
| `seat_layouts` | national_rail_seat_layouts.json | `seat_id` | 國鐵座位配置 |
| `users` | registered_users.json | `user_id` | 乘客帳號 |
| `bookings` | bookings.json | `booking_id` | 國鐵訂票記錄 |
| `metro_travel_history` | metro_travel_history.json | `trip_id` | 地鐵乘車歷史 |
| `payments` | payments.json | `payment_id` | 付款記錄 |
| `feedback` | feedback.json | `feedback_id` | 乘客評價 |
| `policy_documents` | （向量表）| `id` | RAG 政策文件 |

#### 3.1.2 設計決策記錄

| 議題 | 決策 | 原因 |
|------|------|------|

| `metro_stations.lines` 陣列 | `TEXT[]` / 正規化成 `station_lines` | `提升查詢效能與維持第一正規化（1NF）： 雖然 PostgreSQL 支援 TEXT[] 陣列，但地鐵路線常常需要進行「查詢某條路線有哪些車站」的對等連接（JOIN）。拆分出 station_lines 中介表並建立複合索引，能避免全文掃描（Table Scan），且更符合關聯式資料庫的標準規範。 |

| `bookings.status` 欄位類型 | `ENUM` / `VARCHAR` | `保留系統彈性並兼顧資料完整性： 雖然 ENUM 效率極高，但未來若要新增狀態（如 refunded、expired）時，修改 PostgreSQL 的 ENUM 類型較為繁瑣。採用 VARCHAR 搭配 CHECK (status IN ('confirmed', 'cancelled')) 既能阻絕髒資料，又便於未來擴充。` |

| `national_rail_seat_layouts.seats` | 每筆一行 / JSON 欄位 | `避免併發衝突（Concurrency Control）與利於資料鎖定： 若將整班列車的座位塞在單一 JSON 欄位中，當多位乘客同時搶票時，會造成極嚴重的資料覆蓋衝突。拆分成獨立的行（Rows），在執行訂票事務（Transaction）時，可以直接對該座位行進行 SELECT ... FOR UPDATE 悲觀鎖定，確保排他性。 |

| 付款與訂票關係 | `1:1` / `1:N` | `支援失敗重試與多管道支付邏輯： 一張訂單（Booking）在實務上有可能發生第一筆付款失敗、更換信用卡後第二筆才成功的狀況；或者未來可能支援「部分退款/分期支付」。設計為 1:N 能完整保留付款失敗與重試的歷史 log，避免蓋掉重要財務稽核資料。 `|

| `feedback` 類型區分 | 用 `feedback_type` 欄位 / 分成兩表 | `簡化資料庫結構並方便 LLM 整合查詢： 地鐵與國鐵的乘客評價欄位高度相似（皆為使用者、內文、評分、時間）。集中在單一表並以 feedback_type 欄位區隔，能大幅簡化 SQL 複雜度，也更便於 LLM Agent 工具一次性撈取全系統的客服回饋。` |

| 使用者密碼 | 另外新增一個Table / 加密儲存 / Argon2id 雜湊 / 加salt / salt用CSPRNG| `滿足現代資安最高防禦標準： 為了落實最嚴格的密碼安全防護，密碼不能與一般用戶資料混在同一個常用表。應獨立出 user_credentials 表，並採用目前最抗 GPU 爆破與側道攻擊的 Argon2id 演算法，每個帳號的 salt 必須由加密安全偽隨機數生成器（CSPRNG）獨立產生，徹底防範彩虹表攻擊。` |

#### 3.1.3 主要資料表 ER 圖（文字版）

```
users ──1:N──► bookings ──N:1──► national_rail_schedules
                   │                      │
                   └──1:1──► payments     └──1:N──► national_rail_stops
                                          └──1:N──► national_rail_fares

national_rail_schedules ──N:M──► seat_layouts
bookings ──N:1──► seat_layouts

metro_stations ──N:M──► metro_schedules（透過 metro_schedule_stops）
users ──1:N──► metro_travel_history
```

---

### 3.2 Neo4j 圖形 Schema

#### 3.2.1 節點（Node）規格

| 標籤 | 屬性 | 說明 |
|------|------|------|
| `:MetroStation` | `station_id`, `name`, `lines[]`, `zone` | 地鐵站 |
| `:NationalRailStation` | `station_id`, `name`, `city` | 國鐵站 |

#### 3.2.2 關係（Relationship）規格

| 關係類型 | 起點 → 終點 | 屬性 | 說明 |
|---------|------------|------|------|
| `:METRO_LINK` | MetroStation → MetroStation | `line`, `travel_time_min` | 地鐵相鄰站連線 |
| `:RAIL_LINK` | NationalRailStation → NationalRailStation | `line`, `travel_time_min` | 國鐵相鄰站連線 |
| `:INTERCHANGE_TO` | MetroStation → NationalRailStation | `walk_time_min` | 換乘連接 |

#### 3.2.3 設計決策

| 議題 | 決策 |
|------|------|
| 地鐵 / 國鐵站是否合併為一個 Label | 分開（`:MetroStation` + `:NationalRailStation`） |
| 邊上是否存 `fare_per_stop` | 不儲存(關聯式DB計算) |
| 路線方向（有向 / 無向圖） | 無向圖(鐵路為雙向，簡化模型) |

---

### 3.3 pgvector 向量 Schema

| 欄位 | 類型 | 說明 |
|------|------|------|
| `id` | SERIAL | 主鍵 |
| `source` | TEXT | 政策文件來源（e.g. `refund_policy`） |
| `chunk_index` | INT | 文件切塊序號 |
| `content` | TEXT | 原文內容 |
| `embedding` | `vector(768)` 或 `vector(3072)` | 向量（依 LLM 選擇） |

---

## 4. API / 工具函式規格

### 4.1 PostgreSQL Query 函式

> 位於 `databases/relational/queries.py`

| 函式名稱 | 輸入參數 | 回傳格式 | 狀態 |
|---------|---------|---------|------|
| `query_national_rail_availability` | `origin_id, destination_id, travel_date` | `list[dict]` | ⚠️ 待實作 |
| `query_national_rail_fare` | `origin_id, destination_id, fare_class` | `dict` | ⚠️ 待實作 |
| `query_metro_schedules` | `origin_id, destination_id` | `list[dict]` | ⚠️ 待實作 |
| `query_metro_fare` | `origin_id, destination_id` | `dict` | ⚠️ 待實作 |
| `query_available_seats` | `schedule_id, travel_date, fare_class` | `list[dict]` | ⚠️ 待實作 |
| `execute_booking` | `user_id, schedule_id, seat_id, travel_date` | `tuple[bool, dict\|str]` | ⚠️ 待實作 |
| `execute_cancellation` | `user_id, booking_id` | `tuple[bool, dict\|str]` | ⚠️ 待實作 |
| `query_user_bookings` | `user_id` | `dict` | ⚠️ 待實作 |
| `query_user_profile` | `user_id` | `dict` | ⚠️ 待實作 |
| `register_user` | `email, password, ...` | `tuple[bool, str]` | ⚠️ 待實作 |
| `login_user` | `email, password` | `Optional[dict]` | ⚠️ 待實作 |
| `query_policy_vector_search` | `query_text, top_k` | `list[dict]` | ⚠️ 待實作 |

### 4.2 Neo4j Query 函式

> 位於 `databases/graph/queries.py`

| 函式名稱 | 輸入參數 | 回傳格式 | 狀態 |
|---------|---------|---------|------|
| `query_shortest_route` | `origin_id, destination_id` | `dict` | ⚠️ 待實作 |
| `query_cheapest_route` | `origin_id, destination_id` | `dict` | ⚠️ 待實作 |
| `query_alternative_routes` | `origin_id, destination_id, k` | `list[list[dict]]` | ⚠️ 待實作 |
| `query_interchange_path` | `origin_id, destination_id` | `dict` | ⚠️ 待實作 |
| `query_delay_ripple` | `station_id, hops` | `list[dict]` | ⚠️ 待實作 |

---

## 5. 商業邏輯規格

### 5.1 退款計算規則

> 根據 `refund_policy.json`

#### RF001（一般班次）

| 取消時距出發時間 | 退款比例 |
|----------------|---------|
| > 24 小時 | 100% |
| 12 ~ 24 小時 | 75% |
| 6 ~ 12 小時 | 50% |
| < 6 小時 | 0% |

#### RF002（特快班次）

| 取消時距出發時間 | 退款比例 |
|----------------|---------|
| > 24 小時 | 100% |
| 6 ~ 24 小時 | 50% |
| < 6 小時 | 0% |

### 5.2 票價計算規則

#### 地鐵票價

```
總票價 = base_fare + per_stop_rate × 停站數
```

- `base_fare`：1.5 USD
- `per_stop_rate`：0.5 USD/站

#### 國鐵票價

```
總票價 = base_fare(fare_class) + per_stop_rate × 停站數
```

| 艙等 (fare_class) | base_fare | per_stop_rate |
|------------------|-----------|---------------|
| standard | 5 | 1 |
| first | 10 | 2 |

### 5.3 訂票規則

- 一個使用者同一班次同一日期只能訂一張票：**否**
- 取消後座位立即釋出：**是**
- 其他規則：`1、購票時限約束：國鐵訂票僅開放即日起至未來 30 天內之班次，且該班次開車前 15 分鐘即停止線上訂票。`
            `2、座位唯一性：訂票程序（execute_booking）必須包裹在資料庫事務（TRANSACTION）中，若遇到相同日期、相同班次且相同 seat_id 已存在 status = 'confirmed' 的訂單，系統必須立刻回滾（Rollback）並提示座位已被選走。`

---

## 6. 非功能性需求

### 6.1 效能需求

| 項目 | 目標 |
|------|------|
| 單一 Query 回應時間 | < 30 秒 |
| 同時在線使用者（Gradio） | 10 人 |

### 6.2 安全性需求

| 項目 | 規範 |
|------|------|
| 密碼儲存 | 加密 / 雜湊 |
| SQL Injection 防護 | Parameterized Query |
| 未登入使用者限制 | 不可執行訂票 / 取消 |

### 6.3 資料完整性

- 所有外鍵須設定 `ON DELETE` 行為（CASCADE / RESTRICT / SET NULL）
- `bookings.status` 只允許：`confirmed` / `cancelled`
- 付款狀態只允許：`pending` / `completed` / `failed`

---

## 7. 開發環境規格

### 7.1 環境清單

| 工具 | 版本 | 確認 |
|------|------|------|
| Docker Desktop | 最新 | [ ] |
| Python | 3.10+ | [ ] |
| pip 套件安裝 | requirements.txt | [ ] |
| .env 設定 | 已複製並填妥 | [ ] |
| Docker 容器啟動 | 三容器均 healthy | [ ] |

### 7.2 .env 設定項目

```env
# LLM
LLM_PROVIDER=ollama         # gemini 或 ollama
GEMINI_API_KEY=not_required_for_ollama  # 使用本機 Ollama 

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=transitflow
POSTGRES_USER=transitflow
POSTGRES_PASSWORD=transitflow

# Neo4j
NEO4J_URI=bolt://localhost:7688
NEO4J_USER=neo4j
NEO4J_PASSWORD=transitflow
```

### 7.3 本機服務 URL

| 服務 | URL |
|------|-----|
| TransitFlow 聊天介面 | http://localhost:7860 |
| pgAdmin | http://localhost:5051 |
| Neo4j Browser | http://localhost:7475 |

---

## 8. 實作進度追蹤

### 8.1 必做任務

| 任務 | 負責人 | 狀態 | 備註 |
|------|-------|------|------|
| `databases/relational/schema.sql` | | ⬜ 未開始 | |
| `skeleton/seed_postgres.py` | | ⬜ 未開始 | |
| `skeleton/seed_neo4j.py` | | ⬜ 未開始 | |
| `databases/relational/queries.py` | | ⬜ 未開始 | |
| `databases/graph/queries.py` | | ⬜ 未開始 | |

### 8.2 選做任務

| 任務 | 負責人 | 狀態 | 備註 |
|------|-------|------|------|
| 新增 `delay_records` 資料表 | | ⬜ 未開始 | |
| 擴充政策文件 JSON | | ⬜ 未開始 | |
| 修改 agent.py 加入新工具 | | ⬜ 未開始 | |

---

## 9. 驗收測試情境

| 測試案例 | 測試輸入（使用者問題） | 預期行為 | 通過 |
|---------|---------------------|---------|------|
| 查詢國鐵班次 | 「從 NR01 到 NR05 今天有哪些班車？」 | 回傳可用班次列表 | [ v ] |
| 查詢座位 | 「NR1 線明天有哪些標準座位？」 | 回傳空位列表 | [ v ] |
| 訂票 | 「幫我訂 NR01 到 NR05 明天的標準座位」 | 成功訂票並回傳訂單 | [ v ] |
| 退票 | 「取消我的訂單 BK001」 | 計算退款並更新狀態 |  [  ]  |
| 查詢退款政策 | 「我的火車誤點 45 分鐘，我能申請賠償嗎？」 | RAG 搜尋並回答政策 | [  ] |
| 路線規劃 | 「從 MS01 到 MS09 最快怎麼走？」 | 回傳最短路線 | [  ] |
| 換乘規劃 | 「從 MS05 到 NR03 怎麼走？」 | 回傳含換乘的路線 | [  ] |
| 延誤影響 | 「如果 MS05 關閉，哪些站會受影響？」 | 回傳漣漪影響站列表 | [  ] |

---

## 10. 附錄

### A. 資料量統計

| 資料集 | 筆數 | 說明 |
|--------|------|------|
| 地鐵站 | 20 | MS01–MS20 |
| 國鐵站 | 10 | NR01–NR10 |
| 地鐵班表 | — | M1–M4 線 |
| 國鐵班表 | — | NR1–NR2 線 |
| 使用者 | 20 | 虛構帳號 |
| 訂票記錄 | 20 | 虛構資料 |

### B. 參考文件

- `README.md` — 官方英文說明
- `SideNote1-RelationalDBPractices.md` — PostgreSQL 最佳實踐
- `SideNote2-VectorDBPractices.md` — pgvector 最佳實踐
- `SideNote3-GraphDBPractices.md` — Neo4j 最佳實踐
- `train-mock-data/` — 所有原始 JSON 資料

### C. 版本歷史

| 版本 | 日期 | 修改者 | 說明 |
|------|------|-------|------|
| v1.0 | 2026-05-__ | | 初稿建立 |
